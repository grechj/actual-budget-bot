{
  "name": "ab-bot",
  "version": "0.1.0",
  "description": "A local-first Actual Budget AI companion for transaction ingestion, review, and budget recommendations.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "bin": {
    "ab-bot": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/cli.js && node --check src/index.js"
  },
  "keywords": [
    "actual-budget",
    "budgeting",
    "personal-finance",
    "ai",
    "csv-import"
  ],
  "peerDependencies": {
    "@actual-app/api": ">=25"
  },
  "engines": {
    "node": ">=20"
  }
}
